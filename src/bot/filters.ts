/**
 * @file Custom Telegraf filters (if needed).
 * Currently unused, filters are applied directly in handlers.
 */
// This file is left minimal as filters are used directly in handlers for now.
// Add custom Telegraf filters here if reusable logic is needed across handlers.

// import { createFilter } from 'telegraf/filters';

// export const myCustomFilter = createFilter('myCustomFilter');

